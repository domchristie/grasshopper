{
  "name": "grasshopper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.ts --bundle --outfile=dist/grasshopper.js",
    "start": "esbuild index.ts --bundle --outfile=dist/grasshopper.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.1"
  }
}
